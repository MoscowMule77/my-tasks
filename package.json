{
  "name": "sorted",
  "version": "1.0.0",
  "description": "Flit — your life in tabs. Web app + iOS wrapper.",
  "private": true,
  "scripts": {
    "build": "rm -rf www && mkdir -p www && cp index.html styles.css app.js manifest.json www/ && cp -R icons www/icons",
    "ios": "npm run build && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.7",
    "@capgo/capacitor-native-biometric": "^7.6.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
